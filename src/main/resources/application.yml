spring:
  application:
    name: springJPA13Apr24
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:./appdb
    username: sa
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    show-sql: true

